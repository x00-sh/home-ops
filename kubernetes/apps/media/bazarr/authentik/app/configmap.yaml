---
# See kubernetes/apps/media/sonarr/authentik/app/configmap.yaml for the
# pattern this follows.
apiVersion: v1
kind: ConfigMap
metadata:
  name: bazarr-authentik-blueprint
data:
  bazarr.yaml: |
    version: 1
    metadata:
      name: bazarr-forward-auth
    entries:
      - model: authentik_providers_proxy.proxyprovider
        id: provider-bazarr
        identifiers:
          name: bazarr
        attrs:
          external_host: https://bazarr.${SECRET_DOMAIN}
          mode: forward_single
          authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
          invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]

      - model: authentik_core.application
        identifiers:
          slug: bazarr
        attrs:
          name: Bazarr
          provider: !KeyOf provider-bazarr
          meta_launch_url: https://bazarr.${SECRET_DOMAIN}
          policy_engine_mode: any

      - model: authentik_policies.policybinding
        identifiers:
          target: !Find [authentik_core.application, [slug, bazarr]]
          group: !Find [authentik_core.group, [name, media]]
        attrs:
          order: 0
          enabled: true
