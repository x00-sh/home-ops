---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: config-backup
spec:
  dependsOn:
    - name: media-volume
      namespace: media
    - name: volsync
      namespace: storage
  interval: 1h
  path: ./kubernetes/apps/media/config-backup/app
  postBuild:
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: media
  wait: true
