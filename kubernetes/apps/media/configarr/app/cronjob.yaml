---
# Syncs the declarative configuration in configarr-config into Sonarr and
# Radarr once a day. Configarr is idempotent: a run with nothing to change is a
# no-op, so the schedule is about catching drift rather than applying anything.
#
# To run a dry run, add `DRY_RUN: "true"` to env — worth doing before any
# change to the config that touches quality definitions or profiles, since
# the log is the only chance to see what a sync will do before it does it.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: configarr
spec:
  schedule: "30 4 * * *"
  timeZone: Etc/UTC
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        spec:
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            fsGroup: 1000
          containers:
            - name: configarr
              image: ghcr.io/raydak-labs/configarr:1.30.2
              env:
                - name: TZ
                  value: Europe/Amsterdam
                - name: CONFIG_LOCATION
                  value: /app/config/config.yml
                - name: SECRETS_LOCATION
                  value: /app/config/secrets.yml
                # Where the TRaSH guides repo is cloned at runtime. Must be
                # writable, and must NOT be the config mount, which is
                # read-only.
                - name: CUSTOM_REPO_ROOT
                  value: /app/repos
              volumeMounts:
                - name: config
                  mountPath: /app/config/config.yml
                  subPath: config.yml
                  readOnly: true
                - name: secrets
                  mountPath: /app/config/secrets.yml
                  subPath: secrets.yml
                  readOnly: true
                - name: repos
                  mountPath: /app/repos
                - name: tmp
                  mountPath: /tmp
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
                capabilities:
                  drop: ["ALL"]
              resources:
                requests:
                  cpu: 25m
                  memory: 128Mi
                limits:
                  memory: 512Mi
          volumes:
            - name: config
              configMap:
                name: configarr-config
            - name: secrets
              secret:
                secretName: configarr-secrets
            # The cloned guides are a cache, not state: an emptyDir means every
            # run starts from a clean checkout of upstream.
            - name: repos
              emptyDir: {}
            - name: tmp
              emptyDir: {}
