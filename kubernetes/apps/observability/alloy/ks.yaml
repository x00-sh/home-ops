---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: alloy
spec:
  # Loki owns the shared `grafana` HelmRepository, and is also where these logs
  # are sent — no point starting the collector before its destination exists.
  dependsOn:
    - name: loki
      namespace: observability
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: alloy
      namespace: observability
  interval: 1h
  path: ./kubernetes/apps/observability/alloy/app
  postBuild:
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: observability
  timeout: 10m
  wait: true
